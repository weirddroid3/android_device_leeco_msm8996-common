allow hal_sensors_default input_device:dir { r_dir_perms };
allow hal_sensors_default init:unix_stream_socket { connectto };
allow hal_sensors_default sysfs:file { read open };

set_prop(hal_sensors_default, debug_prop)
set_prop(hal_sensors_default, diag_prop)
